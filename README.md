# NLP-Projects

This repository is the parent container for all NLP and GenAI projects.
Each project should be added and tracked as a Git submodule.

## Managing submodules

Add a project:

```bash
git submodule add <repository-url> <relative-path>
```

Initialize and update submodules after cloning:

```bash
git submodule update --init --recursive
```
